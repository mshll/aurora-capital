// import { updateProfile } from '@/actions/users';
// import React from 'react';

// const ChangeImageButton = async ({ onclick }) => {
//   const { image } = await updateProfile();

//   return <button action={onclick(image)}>change</button>;
// };

// export default ChangeImageButton;
